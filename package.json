{
  "scripts": {
    "docker:dev": "docker-compose -f docker-compose.dev.yml up --build",
    "docker:prod": "docker-compose -f docker-compose.dev.yml up --build"
  },
  "dependencies": {
    "@headlessui/react": "^1.5.0",
    "@heroicons/react": "^1.0.5",
    "nodemailer": "^6.7.3"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.4"
  }
}
